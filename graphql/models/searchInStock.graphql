union SearchResult
    @union(resolveType: "App\\GraphQL\\Unions\\SearchResultUnion@resolveType")
    = Ingredient | Preparation

extend type Query @guard {
    searchInStock(keyword: String!): [SearchResult!]!
        @field(resolver: "App\\GraphQL\\Queries\\SearchInStockQuery@resolve")
}
